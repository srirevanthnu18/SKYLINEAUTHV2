[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmd = "pip install -r requirements.txt"

[start]
cmd = "gunicorn app:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"